define('Configuration.Site', [
    'underscore',
    'Configuration'
], function ConfigurationSite(
    _,
    Configuration
) {
    'use strict';

    /**
     * DISCOURAGED: please use SC Configuration record and JSON files
     * In case you really need, you can still do it via code here
     */
    _(Configuration).extend({
    });
});
